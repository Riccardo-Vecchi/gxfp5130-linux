# Local private backups

This directory is for local-only sensor backups such as factory blobs, recovery
traces, and install rollback notes.

Everything in this directory except this README is ignored by Git. Do not publish
its contents: sensor blobs and traces may contain device-specific material.
