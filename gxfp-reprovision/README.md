# Local private reprovisioning material

This directory is for local-only reprovisioning artifacts such as generated PSKs,
`bb010002`/`bb010003` blobs, and command logs.

Everything in this directory except this README is ignored by Git. Do not publish
its contents. A PSK is a device secret; raw fingerprint images are biometric
data.
